# Operating System Scheduler Simulator

A multithreaded C simulator for CPU scheduling, process state transitions, preemption, blocking, and wake-up behavior.

# Contents

`src/scheduler.c`: scheduler implementation
`src/process.c`: process state handling
`src/os-sim.c`: simulator runtime
`Makefile`: build commands
