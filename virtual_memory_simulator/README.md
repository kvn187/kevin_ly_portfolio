# Virtual Memory Simulator

A C virtual-memory simulator covering paging, page faults, swap operations, frame allocation, and page replacement.

# Contents

`implementation/`: paging, fault handling, and replacement implementation
`simulator-src/`: simulator support code
`traces/`: memory-access traces
`Makefile`: build and test commands
