# Cache Simulator

A C cache simulator with configurable access tracking, hit/miss statistics, writebacks, and an LRU stack implementation.

# Contents

`cachesim.c` and `cachesim.h`: cache simulator
`lrustack.c` and `lrustack.h`: LRU replacement support
`lrustacktest.c`: LRU stack test program
